abstract class Routes {
  Routes._();

  static const userview = Paths.userView;
  static const postsView = Paths.postsView;
}

abstract class Paths {
  static const userView = '/userView';
  static const postsView = '/postsView';
}
